const sleep = (ms) => new Promise((r)=>setTimeout(r,ms));
async function fetchJson(url){const r=await fetch(url); if(!r.ok) throw new Error(r.statusText); return r.json();}
async function waitForTarget(){for(let i=0;i<40;i++){try{const list=await fetchJson('http://127.0.0.1:9222/json/list'); const page=list.find(x=>x.type==='page'); if(page?.webSocketDebuggerUrl) return page.webSocketDebuggerUrl;}catch{} await sleep(250);} throw new Error('no target');}
class CDP{constructor(ws){this.ws=ws; this.id=0; this.pending=new Map(); ws.addEventListener('message',(e)=>{const msg=JSON.parse(e.data); if(msg.id&&this.pending.has(msg.id)){ const p=this.pending.get(msg.id); this.pending.delete(msg.id); msg.error ? p.reject(new Error(msg.error.message)) : p.resolve(msg.result);}});} send(method,params={}){ const id=++this.id; const p=new Promise((resolve,reject)=>this.pending.set(id,{resolve,reject})); this.ws.send(JSON.stringify({id,method,params})); return p; }}
(async()=>{ const wsUrl=await waitForTarget(); const ws=new WebSocket(wsUrl); await new Promise((res,rej)=>{ ws.addEventListener('open',res,{once:true}); ws.addEventListener('error',rej,{once:true});}); const cdp=new CDP(ws); await cdp.send('Page.enable'); await cdp.send('Runtime.enable'); await cdp.send('Emulation.setDeviceMetricsOverride',{width:1600,height:1400,deviceScaleFactor:2,mobile:false}); await cdp.send('Page.navigate',{url:'http://127.0.0.1:4173/'}); await sleep(1800); await cdp.send('Runtime.evaluate',{expression:`localStorage.setItem('c4_intro_seen','1'); true`,returnByValue:true}); await cdp.send('Page.reload',{ignoreCache:true}); await sleep(2200); await cdp.send('Runtime.evaluate',{expression:`(() => { const logos=document.querySelectorAll('[aria-label="C4 Studios"]'); logos[logos.length-1]?.scrollIntoView({block:'center'}); return {scrollY:window.scrollY, logos:Array.from(logos).map((el,i)=>{const r=el.getBoundingClientRect();return {i,x:r.x,y:r.y,w:r.width,h:r.height};})}; })()`,returnByValue:true}).then(r=>console.log(JSON.stringify(r.result.value))); await sleep(1000); const shot=await cdp.send('Page.captureScreenshot',{format:'png'}); require('fs').writeFileSync('C:/Users/belac/Downloads/C4-main/C4-main/reports/runtime-audit/footer-full.png', Buffer.from(shot.data,'base64')); ws.close(); })().catch(e=>{console.error(e); process.exit(1);});
